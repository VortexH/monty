#include "monty.h"

/**
 * nop - do nothing
 * @stack: head of stack
 * @line_number: current line number of monty file
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void)(line_number);
	(void)(stack);
}
